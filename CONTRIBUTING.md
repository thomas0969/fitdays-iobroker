# Contributing

Pull Requests sind willkommen. Bitte:

- keine Zugangsdaten oder echten Gesundheitsdaten committen,
- `npm test` ausführen,
- Änderungen an der JSON-Schnittstelle über `schemaVersion` versionieren,
- neue FitDays-Felder nur mit anonymisierten Beispieldaten dokumentieren.
